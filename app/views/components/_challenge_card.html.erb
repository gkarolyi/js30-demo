<div class="col-sm-6 col-md-4 col-lg-3">
  <div class="card-challenge" data-bs-toggle="modal" data-bs-target="#challenge_<%= id %>_modal">
      <%= id %>

    <!-- Modal -->
    <div class="modal fade" id="challenge_<%= id %>_modal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
          <div class="modal-body">
            <%= render partial: "components/challenges/challenge_#{id}" rescue nil %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>